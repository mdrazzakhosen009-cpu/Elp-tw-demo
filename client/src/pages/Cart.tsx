import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import Header from '../components/Header';import Footer from '../components/Footer';import {getCart,removeFromCart,updateQty} from '../lib/cart';
export default function Cart(){const [cart,setCart]=useState(getCart());useEffect(()=>{const f=()=>setCart(getCart());addEventListener('cartchange',f);return()=>removeEventListener('cartchange',f)},[]);const sub=cart.reduce((s,x)=>s+x.price*x.quantity,0);return <><Header/><main className="section"><div className="container"><div className="section-head"><h2>Shopping cart</h2></div>{!cart.length?<div className="notice">Your cart is empty. <Link to="/shop">Continue shopping →</Link></div>:<><div className="grid">{cart.map(x=><div className="card" key={x.productId} style={{padding:16,display:'flex',gap:15,alignItems:'center'}}><img src={x.image} alt="" style={{width:90,height:110,objectFit:'cover',borderRadius:12}}/><div style={{flex:1}}><strong>{x.name}</strong><div>৳{x.price.toLocaleString()}</div><div style={{display:'flex',gap:8,marginTop:8}}><button className="iconbtn" onClick={()=>updateQty(x.productId,x.quantity-1)}>−</button><span style={{padding:10}}>{x.quantity}</span><button className="iconbtn" onClick={()=>updateQty(x.productId,x.quantity+1)}>+</button></div></div><button className="btn btn-light" onClick={()=>removeFromCart(x.productId)}>Remove</button></div>)}</div><div style={{marginTop:25,textAlign:'right'}}><h3>Subtotal: ৳{sub.toLocaleString()}</h3><Link className="btn btn-red" to="/checkout">Proceed to checkout</Link></div></>}</div></main><Footer/></>}
