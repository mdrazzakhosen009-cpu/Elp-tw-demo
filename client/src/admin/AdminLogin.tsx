import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../lib/api';
export default function AdminLogin(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const nav=useNavigate();async function submit(e:any){e.preventDefault();setBusy(true);setErr('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});nav('/admin')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20,background:'#121214'}}><form className="card" style={{width:'min(430px,100%)',padding:28}} onSubmit={submit}><img src="/logo.jpg" alt="" style={{width:70,height:70,borderRadius:'50%',objectFit:'cover',marginBottom:15}}/><h1 className="serif">Trend Wear BD Admin</h1><p className="muted">Secure store management.</p><div className="form"><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div>{err&&<div className="notice error">{err}</div>}<button className="btn btn-dark" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></div></form></main>}
